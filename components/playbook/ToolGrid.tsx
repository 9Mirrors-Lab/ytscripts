import React from "react";nimport { ExternalLink, DollarSign } from "lucide-react";nimport { Badge } from "@/components/ui/badge";nimport { PlaybookTool } from "./PlaybookTemplate";nninterface ToolGridProps {n  tools: PlaybookTool[];n}nnexport const ToolGrid: React.FC<ToolGridProps> = ({ tools }) => {n  return (n    <div className="grid grid-cols-1 sm:grid-cols-2 gap-2">n      {tools.map((tool, index) => (n        <div key={index} className="bg-gray-700/30 rounded-lg p-2 border border-gray-600/30">n          <div className="flex items-start justify-between mb-1">n            <h5 className="text-xs font-medium text-white truncate">{tool.name}</h5>n            {tool.cost && (n              <Badge variant="outline" className="text-xs border-green-500/30 text-green-300 ml-1">n                <DollarSign className="h-2 w-2 mr-1" />n                {tool.cost}n              </Badge>n            )}n          </div>n          <p className="text-xs text-gray-400 mb-1 line-clamp-2">{tool.description}</p>n          <div className="flex items-center justify-between">n            <Badge variant="secondary" className="text-xs bg-gray-600/50">n              {tool.category}n            </Badge>n            {tool.url && (n              <a href={tool.url} target="_blank" rel="noopener noreferrer"n                 className="text-xs text-blue-400 hover:text-blue-300 transition-colors">n                <ExternalLink className="h-3 w-3" />n              </a>n            )}n          </div>n        </div>n      ))}n    </div>n  );n};