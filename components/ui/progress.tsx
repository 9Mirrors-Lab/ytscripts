import * as React from "react";nimport * as ProgressPrimitive from "@radix-ui/react-progress";nnimport { cn } from "@/lib/utils";nnconst Progress = React.forwardRef<n  React.ElementRef<typeof ProgressPrimitive.Root>,n  React.ComponentPropsWithoutRef<typeof ProgressPrimitive.Root>n>(({ className, value, ...props }, ref) => (n  <ProgressPrimitive.Rootn    ref={ref}n    className={cn(n      "relative h-4 w-full overflow-hidden rounded-full bg-secondary",n      classNamen    )}n    {...props}n  >n    <ProgressPrimitive.Indicatorn      className="h-full w-full flex-1 bg-primary transition-all"n      style={{ transform: `translateX(-${100 - (value || 0)}%)` }}n    />n  </ProgressPrimitive.Root>n));nProgress.displayName = ProgressPrimitive.Root.displayName;nnexport { Progress };