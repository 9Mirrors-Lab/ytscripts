import * as React from "react";nimport * as LabelPrimitive from "@radix-ui/react-label";nimport { cva, type VariantProps } from "class-variance-authority";nnimport { cn } from "@/lib/utils";nnconst labelVariants = cva(n  "text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"n);nnconst Label = React.forwardRef<n  React.ElementRef<typeof LabelPrimitive.Root>,n  React.ComponentPropsWithoutRef<typeof LabelPrimitive.Root> &n    VariantProps<typeof labelVariants>n>(({ className, ...props }, ref) => (n  <LabelPrimitive.Rootn    ref={ref}n    className={cn(labelVariants(), className)}n    {...props}n  />n));nLabel.displayName = LabelPrimitive.Root.displayName;nnexport { Label };