import React from "react";nimport { cn } from "@/lib/utils";nninterface GlowCardProps {n  children: React.ReactNode;n  glowColor?: "blue" | "purple" | "green" | "red" | "yellow" | "pink";n  className?: string;n  customSize?: boolean;n}nnconst GlowCard: React.FC<GlowCardProps> = ({n  children,n  glowColor = "blue",n  className,n  customSize = false,n}) => {n  const glowColors = {n    blue: "from-blue-500/20 via-blue-500/10 to-transparent",n    purple: "from-purple-500/20 via-purple-500/10 to-transparent",n    green: "from-green-500/20 via-green-500/10 to-transparent",n    red: "from-red-500/20 via-red-500/10 to-transparent",n    yellow: "from-yellow-500/20 via-yellow-500/10 to-transparent",n    pink: "from-pink-500/20 via-pink-500/10 to-transparent",n  };nn  return (n    <div className={cn("relative", customSize ? "" : "w-full", className)}>n      {/* Glow effect */}n      <divn        className={cn(n          "absolute inset-0 rounded-lg bg-gradient-to-r opacity-0 transition-opacity duration-300 group-hover:opacity-100",n          glowColors[glowColor]n        )}n      />n      {/* Content */}n      <div className="relative z-10">{children}</div>n    </div>n  );n};nnexport { GlowCard };