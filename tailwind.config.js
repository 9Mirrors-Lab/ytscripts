import { fontFamily } from 'tailwindcss/defaultTheme';nnexport default {n  darkMode: ['class'],n  content: [n    './pages/**/*.{ts,tsx}',n    './components/**/*.{ts,tsx}',n    './app/**/*.{ts,tsx}',n    './src/**/*.{ts,tsx}',n  ],n  theme: {n    container: {n      center: true,n      padding: '2rem',n      screens: {n        '2xl': '1400px',n      },n    },n    extend: {n      colors: {n        border: 'hsl(var(--border))',n        input: 'hsl(var(--input))',n        ring: 'hsl(var(--ring))',n        background: 'hsl(var(--background))',n        foreground: 'hsl(var(--foreground))',n        primary: {n          DEFAULT: 'hsl(var(--primary))',n          foreground: 'hsl(var(--primary-foreground))',n        },n        secondary: {n          DEFAULT: 'hsl(var(--secondary))',n          foreground: 'hsl(var(--secondary-foreground))',n        },n        destructive: {n          DEFAULT: 'hsl(var(--destructive))',n          foreground: 'hsl(var(--destructive-foreground))',n        },n        muted: {n          DEFAULT: 'hsl(var(--muted))',n          foreground: 'hsl(var(--muted-foreground))',n        },n        accent: {n          DEFAULT: 'hsl(var(--accent))',n          foreground: 'hsl(var(--accent-foreground))',n        },n        popover: {n          DEFAULT: 'hsl(var(--popover))',n          foreground: 'hsl(var(--popover-foreground))',n        },n        card: {n          DEFAULT: 'hsl(var(--card))',n          foreground: 'hsl(var(--card-foreground))',n        },n      },n      borderRadius: {n        lg: 'var(--radius)',n        md: 'calc(var(--radius) - 2px)',n        sm: 'calc(var(--radius) - 4px)',n      },n      fontFamily: {n        sans: ['var(--font-sans)', ...fontFamily.sans],n      },n    },n  },n  plugins: [require('tailwindcss-animate')],n}