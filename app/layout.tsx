import type { Metadata } from "next";nimport { Inter } from "next/font/google";nimport "./globals.css";nimport { Toaster } from "@/components/ui/sonner";nnconst inter = Inter({ subsets: ["latin"] });nnexport const metadata: Metadata = {n  title: "YouTube Transcript Merge",n  description: "Convert YouTube videos to transcripts in multiple formats",n};nnexport default function RootLayout({n  children,n}: {n  children: React.ReactNode;n}) {n  return (n    <html lang="en" className="dark">n      <body className={`${inter.className} dark bg-background text-foreground antialiased min-h-screen`}>n        {children}n        <Toaster />n      </body>n    </html>n  );n}